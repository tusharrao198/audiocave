from django.urls import path
from . import views
from chatroom.views import *


urlpatterns = [
        # path("", views.index, name="home"),
        # path("chat/<str:room_name>/", views.room, name="room"),
#         path("skipsong/", views.skipSong, name="skip-Song"),
]
